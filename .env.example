# ============================================
# VARIABLES DE ENTORNO - ADMIN-NEXT
# ============================================

# --------------------------------------------
# BACKEND API
# --------------------------------------------
# URL del backend NestJS que expone los microservicios
# Development: http://localhost:3001
# Production: URL de tu servidor desplegado (Railway, Vercel, etc)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# --------------------------------------------
# NPM TOKEN PARA PAQUETES PRIVADOS
# --------------------------------------------
# Token de NPM para acceder a paquetes privados
# Generar en: https://www.npmjs.com/settings/<tu_usuario>/tokens o GitHub (token - classic) w. Packages permissions
NPM_TOKEN=your_npm_token_here

# --------------------------------------------
# THIRDWEB WEB3 AUTHENTICATION
# --------------------------------------------
# Client ID de Thirdweb para conectar wallets
# Obtenerlo en: https://thirdweb.com/create-api-key
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=your_client_id_here

# Dominio donde se ejecuta la app (para auth)
# Development: http://localhost:3000
# Production: https://tu-dominio.com
NEXT_PUBLIC_THIRDWEB_AUTH_DOMAIN=http://localhost:3000

# Private key para firmar JWTs (auth método jwt-cookie)
# NO necesita tener fondos, solo debe ser válida para firmar
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
THIRDWEB_ADMIN_PRIVATE_KEY=your_private_key_here

# --------------------------------------------
# OPCIONAL
# --------------------------------------------
# URL base de la aplicación (para links absolutos)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
